#include <stdio.h>

/**
 * main - Prints numbers from zero to nine.
 *
 * Return: returns 0 
 */
int main(void)
{
	int i;

	for (i = 0; i < 10; i++)
	{
		printf("%d", i);
	}
	putchar('\n');
	return (0);
}

