#include <stdlib.h>
#include <time.h>
/**
 * main - prints a value
 * Description: prints values of a string
 * Return: it returns nothing
 */
int main(void)
{
	int n;

	srand(time(0));
	n = rand() - RAND_MAX / 2;
	/* your code goes there */
	return (0);
}
