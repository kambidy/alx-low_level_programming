#include <stdio.h>
#include "main.h"
/**
 * _puts_recursion - check the code
 *Decription: describes func
 *@s: character inputed
 * Return: Always 0.
 */
void _puts_recursion(char *s)
{
	printf("%s\n", s);
}
